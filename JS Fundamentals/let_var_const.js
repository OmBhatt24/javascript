// ? var functional scope (Global if in outer)
// ? let , const block scoped

// ? If you re-declare a JavaScript variable declared with var, it will not lose its value.
// ? Variables defined with const cannot be Re declared

// ? Variables defined with const cannot be Reassigned -> gives TypeError

var carName = "BMW";
var carName;
console.log(carName);

// const val;
